name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 1
        submodules: true
    - name: Install Clang 15
      run: |
        brew update
        brew brew install --with-toolchain llvm
    - name: make
      run: make
    - name: make test
      run: make test
