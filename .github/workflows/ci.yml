name: C/C++ CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

# Only cancel superseded PR runs (force-pushes / rapid bot updates).
# Never cancel master/main — a new push must not abort an in-flight green check.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

env:
  # CI environments frequently lack privileges / networking setup needed for integration tests.
  # net defaults to running network tests locally; in CI we disable them for determinism.
  NET_DISABLE_NETWORK_TESTS: "1"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    strategy:
      fail-fast: false
      matrix:
        config: [ debug, release ]

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 1

      - name: Verify commit and branch
        run: |
          echo "=== CI Build Information ==="
          echo "Commit SHA: $(git rev-parse HEAD)"
          echo "Commit SHA (short): $(git rev-parse --short HEAD)"
          echo "Commit message: $(git log -1 --pretty=%B)"
          echo "Branch: $(git branch --show-current || echo ${{ github.ref_name }})"
          echo "Commit date: $(git log -1 --format=%ci)"
          echo ""

      - name: Setup LLVM (shared)
        uses: ./.github/actions/setup-llvm

      - name: Verify std.cppm location
        run: |
          if [ -f /usr/lib/llvm-21/share/libc++/v1/std.cppm ]; then
            echo "std.cppm found at /usr/lib/llvm-21/share/libc++/v1/std.cppm"
          else
            echo "Warning: std.cppm not found at expected location"
            find /usr/lib/llvm-21 -name "std.cppm" 2>/dev/null || echo "std.cppm not found anywhere"
          fi

      - name: Show compiler version
        run: clang++ --version

      - name: Diagnostic - Check yar-details file and module discovery
        run: |
          echo "=== Checking yar-details.c++m file ==="
          ls -la YarDB/yar-details.c++m || echo "ERROR: yar-details.c++m not found"
          echo ""
          echo "=== All yar-*.c++m files ==="
          ls -la YarDB/yar-*.c++m || echo "No yar-*.c++m files found"
          echo ""
          echo "=== Build system module discovery (list) ==="
          ./tools/CB.sh ${{ matrix.config }} list | grep -E "yar.*details|yar.*odata" || echo "No yar:details or yar:odata modules found in list"
          echo ""
          echo "=== First 20 yar modules in build order ==="
          ./tools/CB.sh ${{ matrix.config }} list | grep -E "yar" | head -20 || echo "No yar modules found"

      - name: Build project
        run: ./tools/CB.sh ${{ matrix.config }} build

      - name: Run tests with JSONL output
        run: |
          set -euo pipefail
          # Scope to YarDB tests only — full suite includes deps/tester [jsonl-probe]
          # intentional-failure probes and hundreds of dependency tests.
          ./tools/CB.sh ${{ matrix.config }} test --jsonl=failures --tags='\[yardb\]' > test_results_${{ matrix.config }}.jsonl

      - name: Validate JSONL output
        run: |
          set -euo pipefail
          if [ ! -f "test_results_${{ matrix.config }}.jsonl" ]; then
            echo "ERROR: JSONL output file not found!"
            exit 1
          fi
          echo "JSONL lines: $(wc -l < "test_results_${{ matrix.config }}.jsonl")"
          if command -v jq >/dev/null 2>&1; then
            last_line="$(tail -1 "test_results_${{ matrix.config }}.jsonl")"
            echo "$last_line" | jq -e '.type == "eof"' >/dev/null
            summary_line="$(grep '"type":"summary"' "test_results_${{ matrix.config }}.jsonl" | tail -1)"
            echo "$summary_line" | jq -e '.passed == true' >/dev/null
          fi

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-${{ matrix.config }}
          path: test_results_${{ matrix.config }}.jsonl

  yarsh-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 1

      - name: Setup LLVM (shared)
        uses: ./.github/actions/setup-llvm

      - name: Build debug binaries
        run: ./tools/CB.sh debug build

      - name: Run yarsh smoke tests
        run: |
          set -euo pipefail
          ./tests/yarsh/smoke.sh --jsonl | tee yarsh_smoke.jsonl
          grep '"type":"smoke_end"' yarsh_smoke.jsonl | tail -1 | grep -q '"passed":true'

      - name: Upload yarsh smoke results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: yarsh-smoke-results
          path: yarsh_smoke.jsonl

  yardb-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 1

      - name: Setup LLVM (shared)
        uses: ./.github/actions/setup-llvm

      - name: Build debug binaries
        run: ./tools/CB.sh debug build

      - name: Run yardb bind smoke tests
        run: |
          set -euo pipefail
          ./tests/yardb/smoke.sh --jsonl | tee yardb_smoke.jsonl
          grep '"type":"smoke_end"' yardb_smoke.jsonl | tail -1 | grep -q '"passed":true'

      - name: Upload yardb smoke results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: yardb-smoke-results
          path: yardb_smoke.jsonl

  yarexport-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 1

      - name: Setup LLVM (shared)
        uses: ./.github/actions/setup-llvm

      - name: Build debug binaries
        run: ./tools/CB.sh debug build

      - name: Run yarexport smoke tests
        run: |
          set -euo pipefail
          ./tests/yarexport/smoke.sh --jsonl | tee yarexport_smoke.jsonl
          grep '"type":"smoke_end"' yarexport_smoke.jsonl | tail -1 | grep -q '"passed":true'

      - name: Upload yarexport smoke results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: yarexport-smoke-results
          path: yarexport_smoke.jsonl

  yarproxy-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 1

      - name: Setup LLVM (shared)
        uses: ./.github/actions/setup-llvm

      - name: Build debug binaries
        run: ./tools/CB.sh debug build

      - name: Run yarproxy smoke tests
        run: |
          set -euo pipefail
          ./tests/yarproxy/smoke.sh --jsonl | tee yarproxy_smoke.jsonl
          grep '"type":"smoke_end"' yarproxy_smoke.jsonl | tail -1 | grep -q '"passed":true'

      - name: Upload yarproxy smoke results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: yarproxy-smoke-results
          path: yarproxy_smoke.jsonl

  # Static analysis job
  static-analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 1

      - name: Setup LLVM 21 toolchain with analysis tools
        uses: ./.github/actions/setup-llvm
        with:
          packages: "libc++-21-dev libc++abi-21-dev clang-21 clang-tools-21 cppcheck lld-21 lldb-21 jq libssl-dev"

      - name: Run clang-tidy on core files
        run: |
          # Run clang-tidy on key source files
          find . -name "*.c++" -o -name "*.c++m" | head -5 | xargs -I {} sh -c '
            echo "Analyzing {}"
            clang-tidy-21 {} -- -std=c++23 -I/usr/lib/llvm-21/include/c++/v1 2>/dev/null || true
          '

      - name: Run cppcheck
        run: |
          cppcheck --enable=all --std=c++23 --language=c++ \
            --suppress=missingIncludeSystem \
            --suppress=unusedFunction \
            --inline-suppr \
            --quiet \
            YarDB/ deps/tester/tester/ 2>/dev/null || true

  # Submodule health check
  submodule-check:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 1

      - name: Check submodule status
        run: |
          echo "=== Submodule Status ==="
          git submodule status
          echo ""
          echo "=== Checking for uncommitted changes ==="
          if git status --porcelain | grep -q .; then
            echo "✗ Uncommitted changes found in main repo"
            git status --porcelain
            exit 1
          else
            echo "✓ Main repo is clean"
          fi

          # Check each submodule
          for submodule in $(git submodule | awk '{print $2}'); do
            echo "Checking $submodule..."
            if (cd "$submodule" && git status --porcelain | grep -q .); then
              echo "✗ Uncommitted changes in $submodule"
              (cd "$submodule" && git status --porcelain)
              exit 1
            else
              echo "✓ $submodule is clean"
            fi
          done
